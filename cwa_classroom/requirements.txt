Django==4.2.13
mysqlclient==2.2.4
Pillow==10.3.0
stripe==9.11.0
django-storages==1.14.3
boto3==1.34.131
python-dotenv==1.0.1
django-htmx==1.17.3
xlrd==2.0.1
openpyxl>=3.1.0
PyMuPDF==1.24.3
opencv-python-headless>=4.9,<5
numpy>=1.26,<2
anthropic>=0.120.2
openai>=1.40.0
markdown==3.7
requests>=2.31.0
whitenoise>=6.5.0
qrcode[pil]>=7.4.2
gunicorn>=22.0.0
resend>=2.0
# Upper bound deliberate. svix 2.0 changed verify() to return None instead of
# the decoded payload; with `svix>=1.0` a routine deploy picked it up and every
# correctly-signed Resend webhook started 400ing, silently stopping EmailLog
# delivery updates. The view no longer depends on that return value, and this
# bound stops the next major arriving the same way. Tested on 1.99.1 and 2.0.0.
svix>=1.0,<3.0
redis>=5.0.0
django-rq>=2.10.0
phonenumbers>=8.13
# Server-side PDF rendering for the end-of-period progress reports (CPP-388).
# Pure Python — no system libraries to install on the droplet — and its
# graphics package draws the report's charts so the PDF is not a stripped-down
# copy of the web view.
reportlab>=4.2,<5
